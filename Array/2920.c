#define _CRT_SECURE_NO_WARNINGS
#include <stdio.h>

int main()
{
	int cnt = 0;
	int ary[8];
	for (int i = 0; i < 8; i++)
	{
		scanf("%d", &ary[i]);
	}
	for (int i = 0; i < 8; i++)
	{
		if (ary[i] == i + 1)
			cnt++
		else
	}
	for (int i = 8; i > 0; i++)
	{
		if(ary[i == i-7])
	}
	
	

	return 0;
}