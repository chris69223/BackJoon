#include <stdio.h>
#include <stdlib.h>

int main() {
	//char **name = NULL;
	int n, i;
	char *name = (char *)malloc(sizeof(char)*n);
	for (i = 0; i < n; i++)
	{
		name[i] = malloc(n * sizeof(char));
	}

	for (int i = 0; i < n; i++)
	{
		scanf("%s", name[i]);
		strcpy(name,);
	}
}
	
	